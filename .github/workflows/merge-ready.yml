# Disabled for public portfolio display.
# Original workflow was disabled to prevent automatic publishing, privileged
# pull_request_target execution, package writes, or secret-backed jobs in this fork.
name: Disabled - merge-ready

on:
  workflow_dispatch:
    inputs:
      pr:
        description: "Pull request number from rerun dispatchers"
        required: false
        type: string

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is intentionally disabled for the public portfolio repository."
